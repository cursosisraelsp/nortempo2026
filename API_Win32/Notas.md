# Consideracións previas

## Compilación de programa

Para poder executar o programa deberemos compilalo previamente. Da seguinte maneira:

```bash
g++ <nome arquivo>.cpp -o <nome arquivo executable>.exe
```

Lembrar que Windows 11 non permite xerar arquivos, polo que algúns executables deberán ser executados en modo ***administrador***.