#include <iostream>
using namespace std;

int main(){
    int valor = 1; // notas[contador]
    int acumulador = 0; // variará con la suma de las notas

    // primera operación

    acumulador = valor + acumulador; // acumulador = notas[contador] + acumulador
    cout << acumulador << endl;

    acumulador = valor + acumulador; // acumulador = notas[contador] + acumulador
    cout << acumulador << endl;

    acumulador = valor + acumulador; // acumulador = notas[contador] + acumulador
    cout << acumulador << endl;

    acumulador = valor + acumulador; // acumulador = notas[contador] + acumulador
    cout << acumulador << endl;
}